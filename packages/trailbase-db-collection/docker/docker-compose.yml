version: '3.8'

services:
  trailbase:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '4000:4000'
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://localhost:4000/api/healthz']
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s
