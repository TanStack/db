// Full import - query system, localStorage, etc.
import { 
  createCollection, 
  Query, 
  localStorageCollectionOptions,
  createLiveQueryCollection 
} from '@tanstack/db'

console.log(createCollection, Query, localStorageCollectionOptions, createLiveQueryCollection)
