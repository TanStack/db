# Audit scope frozen before readings

Selected queue: fracture-scan, hostile-assay (the requested hostile auditor), loss-audit. No repairs or new architecture selection are part of this audit.

Specimen: representation grammar and first executable separate-result coherence draft, files in source-freeze.json. Declared limits remain limits: no hidden expansion to arbitrary SQL, concurrent response correctness, cross-module dispatch or lifecycle races. The audit also traces where the broad grammar was narrowed in the draft without calling every narrowing a concealed loss.

Fracture: test necessary links under the frozen grammar and under the narrower implementation scope separately. Reject out-of-scope counterexamples as internal fractures.

Hostile: fresh auditor receives only the implemented ownership candidate, contracts, source code and test trace; no sibling ownership form or desired outcome.

Loss: separate scans of detailed grammar/preservation → implementation; multi-table ground conditions → current grammar and implementation; result-sharing evidence → current grammar and implementation. Report absence versus explicit deferral, compression, representation omission and test-generator exclusion. Recover without choosing what to restore.
