# Design Grammar process record

Version 1. Operator: root assistant in the continuing Endpoints inquiry. Selected by the exact current user request in source S2. No independent agent supplied extraction judgments. No production code changed.

## Frozen input and target

[source-freeze.json](../source-freeze.json) hashes the proposal, provisional preservation list and same-system implementation inputs before extraction. The target is the relation among local operation knowledge, read authority, retained collection lifetimes and existing optimistic transaction retirement. It is not a survey of competing sync systems or a request to pick a final protocol.

The exploratory list P1–P12 normalizes existing user requirements and labels added distinctions. No exact-equivalence claim or silent user confirmation is made. Current implementation is supporting context; the proposed protocol itself is the one extraction source.

## Executed stages

1. Read full instrument cards; preserve the selected queue. Fracture runs in the orchestrator for continuity; hostile audit will get a fresh context after this grammar freezes.
2. Freeze source and provisional preservation contract. Read actual runtime, refresh helper and core transaction completion/rollback boundary. Earlier live-query architecture reading remains the governing constraint; no live-query implementation was changed or newly inferred here.
3. Record observations E1–E6 separately from added structure E7. Admit C/M/O/R/F only for reconstruction, exclusions or preservation work. Record overlap L1–L6 instead of forcing ownership into a tree.
4. Reconstruct RC1–RC8. RC6/7/8 expose incomplete capabilities, not a license to implement or claim exact optimism/recovery. Source's ordinary finite quiet example is reconstructible; broader correctness remains conditional and gaps explicit.
5. Ablate each unit, relation and rule. Demote B conditions to context and cohort/retry to derived state; reject a server-version primitive with no sourced contract. Keep U1–U8 without selecting a conflict priority.
6. Separate dynamics, constraints and boundaries. Execute source-derived negative classifications NC1–NC3. Mark independent range RG1 untested; no search for an additional case or use of reconstruction cases as held-out evidence.
7. Generate only F0/F1, with distinct dependency-boundary transformation and loss. No ranking or implementation recommendation.
8. Freeze Model, Evidence and this Process layer to analysis-freeze.json before drafting the primary brief. Projection must use the support map below. Brief text cannot repair the model; changes require a new analytical version.
9. After projection, produce projection-check.json for layer reconstruction. After grammar completion, freeze one F0 candidate for the already selected parallel audit batch. The hostile agent must not read F1, working extraction, fracture output or preferred outcomes.

## Projection support map

| Intended brief claim | Exact analytical support |
| --- | --- |
| Stale-authority design is an evidence/coverage problem rather than choosing a response by arrival | Model L1/L3, I1–I3; E1–E3; RC1–RC4 |
| Handler completion, installed authority and transaction settlement need separate state | C/M/O/R/F, L1; I5; E2/E3/E5; RC4/8 |
| Client quiet interval provides a conditional local route, not a server-wide version | B1–B5, I1; F0; RC2/3; RG1 |
| Retained lifetime and newly loading collections matter beyond subscriber presence | L2/L5, R5/R8, U4/U6; RC5–RC7 |
| Global and proved-independent-domain forms are unranked; unknown impact collapses the second | F0/F1; GF0/GF1; R4/I6 |
| No queue still permits delayed receipts and starvation; unknown outcomes are not resolved | I7; B5; U1/U2/U8; RC3/8 |
| Publication, overlay order, incomplete support and transport/external authority remain open | U1/U3/U4/U6/U7/U8; L4/L6; E3–E6 |
| Extraction adds structure; no new implementation, empirical concurrency test or independent range evidence | E7, RG1, D1–D4; process stages 3–8 |

## Layer reconstruction checklist

P1 → brief action/receipt distinction and Model I7. P2 → brief retained-lifetime paragraph and R5. P3 → completion/authority/retirement paragraph and R2/R7/R9. P4/P5 → observation coverage paragraph and I1–I3. P6 → brief whole-overlay/publication limit and L4/I8. P7 → form-collapse sentence and I6. P8 → isolated versus overlap explanation and R3/R4. P9 → read/lifetime paragraph and R6/R8. P10 → unknown/error paragraph and R2/R9. P11 → conditional progress/publication limits and B5/U3. P12 → no implementation/range/benefit claim and final limitation paragraph. All finer distinctions remain in linked layers; no process-only essential exception is hidden from the brief.

## Later audit handoff boundary

The candidate packet can state F0's own claims and unresolved integration obligations, plus source pointers and current implementation facts. It must omit F1 and constructive preference. Audit findings become separate addenda and cannot rewrite this frozen analytical state. Author-known boundaries remain visible so criticism cannot be manufactured by hiding qualifications.
