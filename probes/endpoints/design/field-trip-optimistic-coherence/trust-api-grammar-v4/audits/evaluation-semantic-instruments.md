# Cross-evaluation: semantic instruments

- Evaluated HEAD: `4ad19815b44be533ac10f90b62aca4377f5138d2`
- Scope: `FS-01`–`FS-06`, `H-01`–`H-12`, and `S-01`–`S-47` (65 source items)
- Evaluation basis: the raw reviews, all seven frozen v4 artifacts, the cited RFC, prior run-57 record, Protocols survey, donor perturbation, Oracle Guide, Field Log requirements, and cited prototype sources.
- Evidence rule: these are documentary/model claims. No candidate API implementation is claimed by the frozen artifacts, so no behavioral issue below is marked reproduced. Prototype code is used only to test documentary statements about what the traced prototype contains. No candidate artifact or central-ledger row was edited.

## Finding ledger

| ID | Technical validity | Exact direct evidence | Suggested final disposition | Duplicate canonical | Durable value / destination | Severity correction |
|---|---|---|---|---|---|---|
| FS-01 | Valid internal fracture. Active obligation and retained condition history are not separated. | `MODEL.md:50-52` says disablement cannot conceal earlier conditions; `:160-166` defines one current attention state; `:228` says `off` changes obligations but erases no conditions. No lifecycle/query split is defined. | `confirmed-open` | — | Add condition occurrence, active interval/disposition, originating/superseding config revisions, and active-vs-history queries to X02/R13. | High |
| FS-02 | Valid omission, not a runtime repro. UC01 assumes the post-edit state it needs. | `PROCESS.md:42-51` begins with M03 already resolved for edited source; `MODEL.md:92-97` makes context change a command and `:227` forbids query mutation. No buffer/context transition or provisional overlay exists. | `confirmed-open` | — | Specify an editor-context ingest command or typed ephemeral overlay, including staleness and reconciliation. | High |
| FS-03 | Valid identity/lowering gap. | `MODEL.md:28-29` retains definition hash; `:36-38` exports independently consequential functions/messages; R04 at `:219` requires semantic-function versions, but lowering at `:147-149` names only M04–M06 plus unspecified presentation relations. | `confirmed-open` | — | Define a deterministic lowering manifest, per-component identity/dependency closure, explicit overrides, and invalidation tests. | High |
| FS-04 | Correctly screened as an explicit open topology, not a hidden defect. | `PROCESS.md:225-226` leaves code review, signatures, local authority files, or another mechanism open; M11 `MODEL.md:106-112` keeps authorship separate from admission. | `design-decision` | — | Resolve C05 before advertising a complete project-local adoption loop. | Informational |
| FS-05 | The rejected rebuttal is correctly rejected: conventional linter simplicity is not a preservation rule. | `PRESERVATION.md:8-50` requires the additional authority/evidence/history distinctions; `MODEL.md:10-20` explicitly says the semantic core is richer than a linter finding. | `refuted` | — | Retain as a scope/control note; no candidate repair follows. | Informational |
| FS-06 | Correctly excluded from this grammar, but it is a real extraction gate later. | `PROCESS.md:179-183` excludes hostile-process security and `:234-236` names loader/sandbox as C10; `EVIDENCE.md:140` repeats the limit. | `deferred` | — | C10 / pre-extraction security design: loader trust, process isolation, signatures, capabilities, and remote execution. | High when extraction begins; out of current scope |
| H-01 | Valid semantic enforcement gap. A record shape plus an asserted authorizer is not an independently anchored authority relation. | M11 is only an “authority act” (`MODEL.md:106-112`) while R12 merely says capability-checked (`:227`); C05 (`PROCESS.md:225-226`) leaves the authority mechanism open. RFC `:549-558` requires ownership/capability/revocation and says it is unspecified; prototype `kernel.ts:186-190` authenticates nothing. | `confirmed-open` | — | Add an external principal/capability/authority anchor and verification relation, or narrow PC4-09/R12 to an external contract. | High |
| H-02 | Valid contradiction unless “envelope” is narrowed to definition-time facts. | M01's immutable envelope retains supersession/admission references (`MODEL.md:24-29`), while M11 later admits, revokes, or supersedes (`:106-112`). Donor `protocol-donor-perturbation.md:140-145` requires separate append/supersede records. | `confirmed-open` | — | Keep the immutable envelope definition-only; represent later admission and supersession as append-only edges projected at a snapshot. | High |
| H-03 | Valid underspecification, but the concrete side effect was not reproduced because no v4 runtime exists. | M02 exports executable applicability and messages (`MODEL.md:34-41`); X05/X06 and R12 promise pure coherent reads (`:182-192`, `:227`). The artifacts do not say whether hooks run at command time, are persisted, or run during reads. | `confirmed-open` | — | Specify purity, dependency capture, isolation/persistence, and the read-time refusal rule for unfrozen inputs. | Medium, not a demonstrated product failure |
| H-04 | The central defect claim is false. Monotonic non-revival is an explicit source rule and does not by itself preclude a separate applicability judgment. | RFC `:333-348` explicitly says revision 7 evidence does not revive at revision 9 even when bytes match; M08 separately gives every use a domain-owned applicability reason (`MODEL.md:85-90`). UC02's broad “configuration revision” wording remains independently weak under S-21. | `refuted` | — | Preserve the narrower S-21 clarification: captured evidence-relevant config slices versus unrelated edits. | Low, not High |
| H-05 | Valid cross-version repair/identity gap. | R10 requires same-law/same-case (`MODEL.md:225`) but no relation object exists; RFC `:395-404` and `:834-839` explicitly leave cross-version mapping, alternate repair, and richer identity unresolved. | `confirmed-open` | — | Add admitted/challengeable equivalence, subsumption, and cross-version case/obligation relations, defaulting unresolved mapping to visible challenge state. | High |
| H-06 | Valid contradiction between UC08 and open threshold ownership. | X02 is policy-free (`MODEL.md:160-166`), X05 is profile-specific (`:182-186`), yet UC08 turns “low-confidence” into one condition (`PROCESS.md:111-121`) while C04 leaves the domain/action threshold boundary open (`:223-224`). | `confirmed-open` | — | Version an admitted domain adequacy rule; reserve profile thresholds for action and record owner/scope/provenance for both. | High |
| H-07 | Valid, fully covered by the earlier disablement fracture. | Same direct conflict as FS-01: `MODEL.md:50-52`, `:160-166`, `:228`; `BRIEF.md:51-54` says `off` deletes neither “the condition” nor evidence. | `duplicate` | FS-01 | Preserve H-07's validity-interval/disposition vocabulary in the FS-01 repair. | High |
| H-08 | Mixed. The grammar promises the complete **returned** batch, not proof that an admitted method exhausts a universe; compromised-method completeness is a separate threat/model question. The detached submit example is underspecified. | M06 requires coverage and omissions (`MODEL.md:66-76`); R06 is expressly scoped to the complete returned batch (`:221`); R08 denies semantic truth from conformance (`:223`). F02 nevertheless shows `observations.submit({batch})` without a run commitment (`:300-305`). | `design-decision` | — | Define which methods promise exhaustive enumeration, who commits the subject universe, and whether external submissions need run receipts/digests. | Medium, not High |
| H-09 | Refuted as a defect in the proposal. The frozen artifacts explicitly call adapter support a model reconstruction and the prototype bounded/incomplete; prototype drift does not contradict proposed X04–X06. | `EVIDENCE.md:115-122` says “Model pass, not executed API test” and range untested; `PROCESS.md:179-186`, `:247-250` names prototype/API losses. The traced LSP does hard-code status/severity (`lsp-server.mjs:13-26`) and one action (`:95-109`), which supports only a future conformance test. | `refuted` | — | Adapter golden/conformance fixtures remain a valuable implementation acceptance suite for X04–X06. | Medium coverage idea, not a present High defect |
| H-10 | Valid transactional-semantics omission. M09 distinguishes delivery/receipt but not intent, attempt, idempotency, or reconciliation after lost delivery. | `MODEL.md:92-97`, R12 `:227`; donor `protocol-donor-perturbation.md:164-168` requires transaction outcomes; RFC `:866-872` leaves transaction/security boundaries open. Prototype `service.mjs:45-130` serializes requests but supplies no idempotency or expected-version identity. | `confirmed-open` | — | Add command intent/attempt/effect/commit/delivery identities, retry-safety declarations, and indeterminate-outcome reconciliation. | Medium |
| H-11 | Valid reproducibility defect in the frozen trace. | `sources.json:14-18` gives S02 only run/event IDs, `:33-37` gives S05 a mutable URL, and `:40-43` paraphrases S06 events; none has bytes or a content hash. Their consequential roles are claimed in `EVIDENCE.md:11-18`. | `confirmed-open` | — | Content-address every consequential source; embed/hash exact restricted/event payloads and aggregate them in a source-set manifest. | High |
| H-12 | Valid. Removal in the candidate vocabulary shows dependency on that factorization, not minimality against merge/substitution models. | `PROCESS.md:123-150` deletes named M/X units and concludes none can be removed; `:211-213` says no other form survived without a candidate ledger. Oracle Guide `GUIDE.md:104-108`, `:239-257` requires a distinguishing wrong answer/failure predicate. | `confirmed-open` | — | Restate use cases in source-level behavior; publish equal-coverage merge/substitution alternatives, metrics, and distinguishing histories. | High |
| S-01 | Valid exact-equivalence loss. Independently admitted adequacy/applicability/presentation roots lack complete definition and lowering identities. | PC4-01 `PRESERVATION.md:8-11`; M11 `MODEL.md:106-112`; R04 `:219`; `TrustRule` lowering names M04–M06 and vague presentation relations only (`:147-149`). | `confirmed-open` | — | Add versioned semantic-function refs and explicit lowering/admission for adequacy, applicability, and presentation, including method-decision-to-premise mapping. | High |
| S-02 | Valid authority-layer collapse in the public sugar. | M03 selects consumer profiles (`MODEL.md:43-48`), M12 owns severity/action (`:114-119`), but the config puts `error`/`warn` inside `rules` (`:256-265`) without typed lowering or distinct authorization. | `confirmed-open` | — | Split activation/configuration from policy or specify separate M03/M12 records and authorization in lowering. | High |
| S-03 | Valid duplicate of the earlier active-condition/history fracture. | `MODEL.md:50-52`, `:160-166`, `:228`. | `duplicate` | FS-01 | Carry its `activeConditions`/`conditionHistory` naming into FS-01. | Medium (canonical issue High) |
| S-04 | Valid preservation gap. M07 includes some context but not the full PC4-03 record. | PC4-03 requires evaluator, schema, input, calibration, provenance, trust roots, coverage, omissions, results (`PRESERVATION.md:14-17`); M07 lists method version, context, reach, coverage, omissions, operational result only (`MODEL.md:78-83`). Prior run 57 P05 explicitly retained producer/run/payload/trust roots/limits/provenance (Field Log event 872). | `confirmed-open` | — | Expand M07 or its mandatory references with producer/run/provenance/trust roots/evaluator/schema/calibration; keep operation outcome in M09. | Medium |
| S-05 | Valid duplicate of the cross-version identity/repair gap. | `MODEL.md:85-90`, `:160-165`; RFC `:309-323`, `:834-839`. | `duplicate` | H-05 | Preserve proposer/status/reason/authority and challenge history in the H-05 relation object. | High |
| S-06 | Valid conditional failure-provenance loss. | M09 names coarse phases (`MODEL.md:92-97`), while prior run 57 P07 retained primary failure, cleanup diagnostics, resource state, returned batch, and relevant invocation/measurement/delivery/application/publication/settlement phases (Field Log event 872). Oracle Guide `:261-276` separates primary failure, cleanup, and resource release; C06 (`PROCESS.md:227-228`) admits the durable home is unresolved. | `confirmed-open` | — | Restore typed conditional phase/failure/cleanup/resource fields or explicitly record the deliberate v2 loss. | Medium |
| S-07 | Valid causal-replay gap. | M10/R10 retain law/case/use/applicability/happens-after (`MODEL.md:99-104`, `:225`) but do not require a fresh non-cache measurement or exact check/rule versions. RFC `:364-387` expressly rejects pre-failure, same-batch, cached, and unrelated passes. | `confirmed-open` | — | Add `startedAfterObservation`, exact check/rule/case versions, fresh-measurement witness, and same-violation relation. | High |
| S-08 | Valid in its protocol-revision core; the review overstates the absence of every evidence/challenge path. | PC4-09 explicitly includes evidence, challenges, repairs, and protocol revisions (`PRESERVATION.md:32-34`). M11 admits listed semantic roots but not protocol revisions (`MODEL.md:106-112`); M07/M09/M10 provide records but no uniform proposal lifecycle. | `confirmed-open` | — | Enumerate proposal records/commands for all PC4-09 categories, especially protocol revision, or narrow the contract. | Medium |
| S-09 | Valid ownership ambiguity/contradiction. | M04 owns human/agent presentation hooks (`MODEL.md:54-58`) and PC4-01 makes presentation domain-owned, but M12 gives consumers unqualified “presentation choices” (`MODEL.md:114-119`). RFC `:147-153` limits policy to permission/severity/fallback/enforcement. | `confirmed-open` | — | Restrict M12 to channel/visibility/severity/gating/fallback; leave semantic wording, meaning, and source mapping domain-owned. | Medium |
| S-10 | Valid taxonomy loss. | X01/X02 (`MODEL.md:153-166`) contain missing evidence and unreachable reach but no explicit absent-vs-inconclusive observation or cleanup/resource failure. RFC `:282-301` separates those states; run 57 had `TRUST_OBSERVATION_INCONCLUSIVE` and `TRUST_OPERATION_FAILED` (Field Log event 872). | `confirmed-open` | — | Add distinct absent, inconclusive, unreachable, operation-failure, and cleanup/resource-failure variants. | High |
| S-11 | Valid broader lifecycle gap, not merely disablement. | PC4-06 requires a distinct condition lifetime (`PRESERVATION.md:23-25`); X02 only calls a condition stable (`MODEL.md:160-166`) and defines no creation, activation, resolution, supersession, or expiry. Donor A-T3 (`protocol-donor-perturbation.md:76-86`) distinguishes condition appearance/disappearance from durable history. | `confirmed-open` | — | Define condition identity and lifecycle independently from evidence, config, work leases, and override expiry. | High |
| S-12 | Valid terminology defect. | `MODEL.md:208-210` calls the overlaps a semilattice but supplies no order, join, closure, associativity, commutativity, or idempotence—only the overlap table at `:196-206`. | `fixed-now` | — | Rename it an overlap/dependency hypergraph unless formal laws are added. | Low |
| S-13 | Valid frozen-priority contradiction. | `MODEL.md:228`, `:279-282` categorically maps source-local suppression to M12; C03 `PROCESS.md:221-222` says its classification remains open; `:238` says no priority was invented. | `fixed-now` | — | Revert categorical language to the explicit C03 alternatives until selection evidence exists. | High |
| S-14 | Valid matrix/documentation omission. | Dependency matrix `MODEL.md:235-244` omits M01, while M01 supplies cross-interface/version identity (`:24-32`) and X06 explicitly contains a version (`:188-192`). | `fixed-now` | — | Add M01 or state that envelope identity is a cross-cutting implicit dependency. | Low |
| S-15 | Valid presentation ambiguity, but the invented names are illustrative rather than an implemented policy. | Representative config uses `standard`, `strictCI`, and `developmentPolicy` (`MODEL.md:246-265`) while C09 leaves names, required sets, authority, expiry/review open (`PROCESS.md:232-233`). | `fixed-now` | — | Label names as hypothetical metavariables and cross-reference C09. | Low, not Medium |
| S-16 | Valid derived overclaim. | Preservation map marks every row supported (`EVIDENCE.md:24-37`), despite missing PC4-03 fields (S-04), explicit PC4-04 relation (H-05), PC4-06 lifecycle (S-11), and PC4-09 protocol proposal (S-08). | `confirmed-open` | — | Mark affected cells partial/failing until their canonical model gaps close. | High, not Medium |
| S-17 | Valid provenance-category correction. The transfer is explicitly an analyst mapping even when its donor fit is solid. | E-O03 is under “Source-stated” (`EVIDENCE.md:41-50`), but donor A-T1 calls the relation a “transfer” (`protocol-donor-perturbation.md:62-74`) and the v4 process says conditions/work were analyst-introduced (`PROCESS.md:240-245`). | `fixed-now` | — | Move E-O03 to donor-transferred/analyst-inferred and preserve its fit/limits. | Medium |
| S-18 | Valid smaller provenance-category correction. | E-O04/O05 are “Source-stated” (`EVIDENCE.md:50-53`), while donor U-T1 and I-T1 are explicit Trust transfers (`protocol-donor-perturbation.md:120-126`, `:178-190`) with fit labels, not home-domain primary facts. | `fixed-now` | — | Reclassify as donor-transferred inferences while retaining solid/plausible fit labels. | Low |
| S-19 | Valid duplicate of the frozen C03 contradiction. | `EVIDENCE.md:83-86` selects policy while `PROCESS.md:221-222` leaves classification open. | `duplicate` | S-13 | Preserve the evidence-layer repair in the canonical S-13 change. | High |
| S-20 | Refuted as stated. The section never claims to be an exhaustive disposition ledger, and exact-equivalence is bounded by the preservation contract. | Heading is only “Source claims deliberately not transferred” (`EVIDENCE.md:94-109`); bounded completeness is defined at `PRESERVATION.md:47-50`. The cited survey strands do exist (`protocols-institute-hardness-survey.md:67-116`) and A-T4 raises hold/objection time (`protocol-donor-perturbation.md:88-92`), but no completeness claim says all source themes must be listed. | `refuted` | — | An optional comprehensive source-disposition appendix would improve auditability, but it is not a current contradiction. | Low, not Medium |
| S-21 | Valid wording/applicability gap, narrower than H-04. | UC02 says a changed dependency/config revision yields expiry (`PROCESS.md:55-64`), while M08 limits context to revisions relevant to an observation (`MODEL.md:85-90`) and RFC `:345-348` makes invalidation selective but only as complete as discovery. | `confirmed-open` | — | Say “changed captured, evidence-relevant configuration dependency” and specify resolved slices. | Low |
| S-22 | Valid reconstruction failure. | UC06 evaluates proposed M03/M12 inputs and returns new work (`PROCESS.md:92-98`), but X05 accepts one current snapshot/profile/active overrides only (`MODEL.md:182-186`); no candidate definition/config closure or nonpersistent work projection exists. | `confirmed-open` | — | Add a sealed counterfactual bundle/overlay with definition closure and distinguish preview work from durable X03. | High |
| S-23 | Valid operation/projection gap. | UC07 relies on capability/schema discovery (`PROCESS.md:100-109`); M02 only says definitions become discoverable (`MODEL.md:34-41`), while negotiation is merely an F03 option (`:313-323`), not a shared invariant/operation. | `confirmed-open` | — | Add resolved module/rule/schema/capability discovery to the common service or remove the UC07 step. | Medium |
| S-24 | Valid duplicate of the adequacy/action-threshold contradiction. | `PROCESS.md:111-121`, C04 `:223-224`, `MODEL.md:160-166`, `:182-186`. | `duplicate` | H-06 | Preserve the “outside admitted/calibrated range” vocabulary in H-06. | Medium (canonical issue High) |
| S-25 | Valid false no-loss claim. | `PROCESS.md:154-159` says no v2 semantic distinction was removed; run 57 P05/P07 and its stable codes retain absent-vs-inconclusive, operation/cleanup/resource, returned-batch, and capability/operation families (Field Log event 872), several absent from v4 S-06/S-10/S-23. | `confirmed-open` | — | Restore the distinctions or enumerate each deliberate compression/loss and weaken exact-equivalence/control status. | High |
| S-26 | Valid incomplete conflict accounting. | C01–C10 (`PROCESS.md:215-236`) omit RFC-open semantic equivalence/subsumption/cross-version identity, objection class, alternate repair, selective expiry/distributed clocks (`RFC:828-839`) and donor hold/objection time (`protocol-donor-perturbation.md:88-92`). | `confirmed-open` | — | Expand the unresolved register or trace a supported resolution for every omitted conflict. | Medium |
| S-27 | Valid label defect. | The support row says “Editor, daily renewal, CI, and downgrade examples” but cites UC01–UC06 (`PROCESS.md:264-274`), omitting UC04 Devtools and UC06 preview from its label. | `fixed-now` | — | Name all six cases. | Low |
| S-28 | Valid imprecise compression. | `BRIEF.md:31-35` calls config “evidence-relevant history” and admission an identity “underneath”; M03 says config controls applicability/obligations (`MODEL.md:43-52`) and M11 is a separate authority record (`:106-112`). | `fixed-now` | — | Use “applicability-relevant configuration history” and “independently addressable admission records.” | Low |
| S-29 | Valid brief-level causal compression loss. | `BRIEF.md:45-47` names authorization/applicability/later/same law-case, but R10 also requires affected use (`MODEL.md:225`) and RFC `:364-387` requires fresh post-failure, non-cache replay. | `fixed-now` | — | Add affected-use/fresh-replay qualifiers or link the sentence explicitly to R10 plus the replay rule. | Medium |
| S-30 | Valid duplicate of the broad condition-lifecycle gap. | `BRIEF.md:48-50` lists clocks but omits the contract's condition lifetime (`PRESERVATION.md:23-25`). | `duplicate` | S-11 | Add condition lifetime to the brief when S-11 is repaired. | High |
| S-31 | Valid duplicate of the disablement fracture. | `BRIEF.md:51-54` says `off` changes obligations yet “neither deletes the condition,” without active/history qualification. | `duplicate` | FS-01 | Correct the brief as part of FS-01: retain condition history; define current activation separately. | High |
| S-32 | Valid primary-brief limit loss. | `BRIEF.md:56-66` omits applicability ownership, operational-failure home, and unmeasured real agent/editor/CI/Devtools effectiveness; `EVIDENCE.md:132-142` explicitly lists those limits. | `fixed-now` | — | Add one concise limits sentence carrying those takeaway-changing uncertainties. | Medium |
| S-33 | Valid duplicate of the public M03/M12 collapse; its placeholder-name concern is separately S-15. | `BRIEF.md:70-78` embeds `error`/`warn` in rules without lowering; same as `MODEL.md:256-265` against M12 `:114-119`. | `duplicate` | S-02 | Apply the S-02 separation/lowering to both MODEL and BRIEF; cross-reference S-15 for hypothetical names. | High |
| S-34 | Valid precision edit, not evidence that selection currently occurs. | `BRIEF.md:81-83` says singular “complete observation”; R06 says complete returned batch (`MODEL.md:221`). | `fixed-now` | — | Say “method's complete validated returned observation batch.” | Low |
| S-35 | Valid derived control-status defect. | `frozen-analysis.json:15-21` marks all UC01–08 reconstruction and every survivor model-pass; S-22/S-23/H-06 show UC06/07/08 gaps, and S-16 shows preservation gaps. | `confirmed-open` | — | Mark affected controls partial/failing and rerun documentary reconstruction after canonical repairs. | High |
| S-36 | Valid source-description/provenance defect. | `sources.json:40-43` records only the extension requirement; Field Log event 1038 says merely “linting for agents,” event 1041 says packages/enable-disable/local authoring, while E-O07 claims the expanded idiom list (`EVIDENCE.md:57-59`). | `fixed-now` | — | Quote/hash the exact events and label expanded linter idioms as interpretation unless separately confirmed. | Low |
| S-37 | Valid evidence gap. The frozen contract attributes an expanded linter idiom list to the user beyond the verbatim comments. | Field Log event 1038 supplies the analogy and event 1041 extension model; generated event 1044 adds stable codes, locations, severity, suppression, fixes, machine output, and CI status as `basis:"user"`; `PRESERVATION.md:38-46` freezes that expansion. | `confirmed-open` | — | Obtain user confirmation or mark the expanded idioms analyst-interpreted/user-compatible rather than verbatim-required. | Medium |
| S-38 | Valid duplicate of the same linter-requirement provenance gap. | E-O07's full list (`EVIDENCE.md:57-59`) exceeds verbatim Field Log events 1038/1041. | `duplicate` | S-37 | Reclassify E-O07 when S-37 is resolved. | Medium |
| S-39 | Valid duplicate of the ablation/minimality defect. | `PROCESS.md:123-150` is removal-only and does not ablate O01–O09 or test redistributed jobs. | `duplicate` | H-12 | Add overlap ablations and merge/redistribution countermodels to H-12's control redesign. | High |
| S-40 | Refuted. `TrustRule` is uniquely retained despite adding no semantic state; M02/M03 preserve install/enable/config/admission distinctions and X04 preserves a frozen diagnostic contract. | `PROCESS.md:127-145` gives semantic/contract failures for M02/M03/X04; `:147-150` says only `TrustRule`'s necessity is ergonomic and that it lowers to semantic units. | `refuted` | — | No change required; an explanatory sentence could preempt the confusion. | Low, not Medium |
| S-41 | Valid duplicate of H-12's unsupported minimality/compression claim. | `PROCESS.md:161-167` says “shorter than” with no size metric or normalized alternatives. | `duplicate` | H-12 | Include an explicit measure and equal-coverage alternatives in H-12's destination. | Medium |
| S-42 | Valid duplicate of H-12's missing candidate-generation evidence. | `PROCESS.md:211-213` says no additional form survived but records only three cosmetic classes, not candidates and distinction tests. | `duplicate` | H-12 | Add candidate/rejection ledger or soften the claim under H-12. | Medium |
| S-43 | Valid duplicate of the frozen-source reproducibility defect. | `sources.json:33-37` gives S05 only a mutable URL and limit, with no bytes/hash/retrieval timestamp. | `duplicate` | H-11 | Preserve S05-specific legal snapshot/hash and timestamp in H-11's source manifest. | Medium |
| S-44 | A legitimate unresolved API factoring question, not a current defect. | M04 places `ruleCode` on `ClaimDefinition` (`MODEL.md:54-58`); nothing says whether a claim may appear under multiple authored rule/presentation identities. | `design-decision` | — | Decide claim reuse cardinality; if many-to-many, put code on rule/use/presentation identity and test stable cross-interface references. | Low |
| S-45 | Refuted. The example is visibly project-local and the same artifacts repeatedly disclaim second-domain range. | Comment and export identify `trust/rules/orders-preserve-ledger.ts` (`MODEL.md:267-276`); `PROCESS.md:179-186` and `EVIDENCE.md:122-130` say Endpoints-only/untested. | `refuted` | — | Optional “illustrative” label is harmless, but no range claim currently follows. | Informational, not Low defect |
| S-46 | Valid freeze-integrity improvement. The source set hashes some inputs, but the seven output artifacts and evaluated base/tree are not content-addressed. | `frozen-analysis.json:1-41` has IDs/file names but no artifact digests/base commit; `sources.json:1-81` hashes only path-backed inputs. At evaluation, all seven candidate artifacts are untracked at HEAD. | `confirmed-open` | — | Add candidate-artifact hashes, base/tree identity, aggregate manifest hash, and a validation command. | Low |
| S-47 | Valid duplicate of the broader source-trace reproducibility defect. | `sources.json:14-18`, `:33-49` uses Field Log IDs but names no `field_log.jsonl` path or payload hashes for S02/S05/S06/S07. | `duplicate` | H-11 | Add Field Log path and canonical event-payload hashes to H-11's manifest. | Medium, not Low |

## Unresolved evidence gaps

- H-03 remains a model-level gap: no v4 implementation exists in which to reproduce an impure read, and the artifacts do not choose when semantic hooks execute.
- H-08 needs an explicit threat/coverage contract: “complete returned batch,” exhaustive subject enumeration, and detached external submission are three different promises.
- S-37/S-38 require provenance clarification or user confirmation for the expanded linter idioms. The exact comments are available, so the gap is interpretation/authority rather than missing bytes.
- S05/Jev article-specific claims remain independently unreproducible from the frozen trace. No live-page result was used here.
- No item lacks direct documentary evidence for the disposition above. No behavioral defect is claimed reproduced.

## Reviewer and instrument quality

| Instrument | Accuracy | Depth | Signal | Fix quality | Assessment |
|---|---|---|---|---|---|
| Fracture Scan (`FS`) | High | High for a six-item bounded scan | Very high | High | It correctly isolates three real internal fractures, screens C05 instead of inflating it, and uses two honest controls. Its main limitation is that the fractures are model counterexamples rather than executable failures, which it states. |
| Hostile Assay (`H`) | Medium-high | Very high | High | High | It finds serious authority, immutability, threshold, identity, retry, source-freeze, and minimality gaps. It overreaches on monotonic revision history (H-04), treats explicit proposal/implementation limits as adapter contradiction (H-09), and blurs returned-batch integrity with method completeness (H-08). |
| Semantic/Loss Audit (`S`) | High | Excellent | High, with some duplicate and scope noise | High | It is lossless, source-literate, and especially strong on exact-equivalence gaps and projection inconsistencies. Its weak spots are inferred exhaustiveness (S-20), treating semantic primitives with interface consequences as merely ergonomic (S-40), and reading an explicitly local example as possible range evidence (S-45). Several rows correctly rediscover earlier canonical issues and are preserved as duplicates. |

**Semantic reviewer hire recommendation:** **Yes—strong hire for design-grammar and documentary audit work.** The reviewer shows unusually good preservation discipline, cross-layer tracing, and repair specificity. Calibration should focus on distinguishing a proposed semantic contract from implementation evidence, not imputing “complete ledger” claims without text, and canonicalizing repeats earlier without losing their unique evidence.

## Loss audit

Disposition totals:

| Disposition | Count |
|---|---:|
| `fixed-now` | 12 |
| `confirmed-open` | 29 |
| `already-fixed` | 0 |
| `stale` | 0 |
| `refuted` | 6 |
| `deferred` | 1 |
| `design-decision` | 3 |
| `duplicate` | 14 |
| **Total** | **65** |

`assigned items 65 = fixed-now 12 + confirmed-open 29 + already-fixed 0 + stale 0 + refuted 6 + deferred 1 + design-decision 3 + duplicate 14`

Every assigned ID appears exactly once. All 14 duplicates name one canonical ID and retain their unique evidence/destination. The evaluated HEAD remained `4ad19815b44be533ac10f90b62aca4377f5138d2`; only this cross-evaluation file was added by this task.
