# Process and projection trace — frozen v1

User selected a fresh Design Grammar with “yes” after explicitly rejecting compiler auth rewriting. Recorded selection run26. Only this instrument is selected; no fracture/hostile/research or implementation follows automatically.

Frozen input: PRESERVATION.md was saved before extracting the model and running the compiler controls. Explicit user properties were normalized without another approval question. Analyst consequences are provisional; this is not an exact-equivalence run.

Source boundary: one current Endpoints analysis/emission/consumption system. Local source spans and hashes are in sources.json. The prior uninstrumented draft is not grammar evidence. Six synthetic cases use existing compiler fixture machinery and PGlite only to supply a schema snapshot. No installed auth library, credentials, external API, browser, app database, or application module execution was used.

Operation record: read selected card; inspect source path; save preservation; run controls; correct fixture binding mismatch; remove an unasserted reporting field and add a real naming equality assertion; write observations; extract U/L/R items; reconstruct existing compiler flow; ablate each unit/relation/rule; retain active evidence-consumer and evidence-diagnostic overlaps; reject forbidden/cosmetic forms; generate two distinct composable extensions; mark independent range untested. Controls and failures are in EVIDENCE.md. No agents were needed because no independent held-out range case was supplied.

Freeze: save Model, Evidence and Process, then hash those plus preservation, source ledger and controls in freeze.json before writing BRIEF.md. A substantive addition during projection requires a new frozen version rather than backfitting evidence to prose.

Pre-projection claim support map:
B1: No special auth primitive; source code remains owner — P1/P3,U1,R1,E2/E5,N2/N3.
B2: Claim kind differs from its consumer's permission — U2/U3,L2,R2/R5,E1/E6,C1.
B3: One evidence basis can drive conservative runtime decisions and useful lint — U2/U4,L3/L4,R3/R6,E3/E4,C2.
B4: Two unranked, composable extensions — F1/F2, ablations and generation exclusions.
B5: Example unknown helper cannot become recipes-only proof — E3/N1,R3; diagnostics are illustrative, not implemented output.
B6: Limits are binding — E0/E5/RC2, P5/P8/P9,C1/C3/C4/C5, independent range untested, no full-stack correctness/performance claims.

Layer-reconstruction check after projection must recover B1–B6 in the brief and P/U/L/R/C through linked layers. No new request-context API, lint severity policy, library trust mechanism, implementation priority or ranking may be added during projection.
