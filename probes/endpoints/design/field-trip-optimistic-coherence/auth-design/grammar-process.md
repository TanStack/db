# Process — frozen v1

Selected sequence: ground condition run22 → exploratory grammar run23 → fracture24 and fresh hostile25 in parallel. User said “ok go”. This is examination authority, not implementation/adoption authority.

Extraction source and provisional preservation list are fixed in Model P1–P10; source hashes/coverage in sources.json. Ground-condition findings E1–E5 entered extraction. The source/control observations are E1–E5; units, legal rules and F1–F3 are analyst readings constrained by those facts. No exact-equivalence claim.

Admission and pruning: each U/L/R/P survives the explicit ablation ledger; named rejected units were demoted. Overlap retained instead of forcing session/auth/payload into independent modules. Reconstruction E6 is partial for timing and header delivery; that loss is carried into F2/F3 and the brief. E7 is the near-negative; E8 marks no independent range. No hidden range search or contaminated evaluator.

Freeze order: write Model and Evidence; write this Process and the isolated F2 candidate; hash all into grammar-freeze.json; only then project brief. Any added claim requires a new freeze version.

Projection support map (fixed before brief):
- B1 permission/session operation differs from payload: U2/U3/L2/L3, E1–E3.
- B2 auth policy/effects cannot vanish: P2–P5, R1/R2, E2/E5, C2.
- B3 three unranked forms and coordination cost: F1/F2/F3, E10.
- B4 unknown ordinary helpers and privacy boundary: P6/P7,R2/R3,E5/E7.
- B5 exploratory scope, missing identity/denial/transport and range: P8,C1–C5,E4/E6/E8.
- B6 no performance claim or implementation: C4, E8/E10.

Layer reconstruction check to perform after projection: B1–B6 must appear in brief; supporting layers recover every P/U/L/R/C and each negative/range/reconstruction result. No priority among C1–C3 is supplied.

Audit subject selection: freeze F2 as the one testable adjacent split, because it makes an explicit control boundary to attack. This does not rank it above F1/F3 or adopt it. The hostile auditor receives only audit-candidate.md, sources.md/json, the probe/results and allowed code; no Model/Process/Brief/other findings. Orchestrator fracture uses the exact same candidate. Independent range testing is not part of that assay.
