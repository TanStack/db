# Portable source copies

The [manifest](manifest.json) maps historical absolute paths to checked-in copies
of the RFC and instrument cards used in this inquiry. Frozen records retain the
paths and bytes they originally referenced; do not rewrite them to suit a new
machine.

Historical paths under `/Users/kylemathews/.codex/worktrees/e079/tanstack-db/`
map directly to the same repository-relative path in a new checkout. The Process
Grammar's [source capture](../process-grammars/sources.json) embeds the selected
Kitchen AI passages and their original hashes. Running the evidence-base
prototype does not require that separate app or its database.

The Field Log is readable as ordinary Markdown and canonical JSONL without any
local service. To append to it, use the Field Lab writer from an installed copy
of the skill; do not edit the two generated/canonical log files by hand. The
old localhost reader URLs and machine-specific tool paths are not prerequisites
for reading or testing this checkout.

These copies preserve source procedures, not a bundled installation of the
Field Lab application. References within the copied cards to other resources
remain provenance pointers, not runtime dependencies.
