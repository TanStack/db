# Stage 3 Editorial Candidate Map trace — limits and evolution

## Generation boundary

- Fresh, sibling-hidden pass.
- Read only the Editorial Candidate Map instrument, the frozen Stage 3 ledger, and its three canonical artifacts.
- No expectation output, blind probe, ranking, validation, recommendation, applicability choice, outside theory, or external source entered generation.
- IDs below are ordering labels only. All retained samples remain unvalidated and unranked.
- Coordinate assignment: audit/repair history, conservative boundaries, unresolved questions, evidence gaps, implementation state, and Endpoints only as a bounded worked consumer.

## ECM-LE-01 — The architecture as a repair record

**Public claim**

The current semantics—AND/OR argument routes, monotonic freshness, persistent challenges, and causally later exact replay—are the residue of concrete failed moves and bounded repairs, not an arbitrary list of primitives.

**Upstream inputs and exact support**

- `BR-02`, `BR-04`: the RFC must make architecture, semantics, evidence lifecycle, implementation state, and unresolved decisions legible, including how evidence becomes or fails to become support.
- `ESS-S12`: Field Log `3984–4117, 6297–6335`; v2 README `1–39`; HANDOFF `1–11, 94–138`. Historical sequence from exploratory grammar through fracture/hostile findings, v2 repair, and the later rebuild.
- `ESS-H03`: Field Log `4054–4117, 6297–6319`; v2 README `9–39`; v2 `evidence.json`; `kernel.test.mjs` `388–558`. The first explanation erased AND/OR completion sets, repair authority survived stale context, and delivery order could impersonate replay causality.
- `ESS-S03`: grammar `source.md` `64–74`; v2 README `9–13, 29–32`; v2 `model.json` `E2/R2/R8`; `kernel.ts` `570–704`; tests `92–128, 289–306, 388–438`. Repaired AND/OR support and cycle exclusion.
- `ESS-S04`: grammar `source.md` `98–106`; v2 README `14–27`; v2 `model.json` `E5/R4–R7`; `kernel.ts` `468–568`; tests `130–213, 440–558`; Field Log `4054–4117, 6297–6319`. Append-oriented challenge and exact-replay lifecycle.
- `LGI-V2-18`: v2 README `34–39`; v2 `evidence.json` red `4–16`, green `18`, oracle repairs `19–22`; `process.json` control `15`. Specific repair witnesses and controls were compressed to aggregate counts on the public surface.
- `ESS-E01`, `LGI-V2-19`: independent range, a new full Design Grammar run, post-repair hostile audit, and agent-workflow effectiveness remain untested.
- `ESS-E13`: the later rebuild is runnable working-tree state, not a committed or released package.

**Control-ledger status**

- `ESS-H03` is a supporting shadow, not a promoted requirement or an omission bonus.
- `LGI-V2-18` is a revision-specific supported recovery with `category mismatch`; it remains observed repair evidence/control, not a claim of general validation.
- `ESS-E01`, `ESS-E13`, and `LGI-V2-19` are gaps or explicit no-claims, not positive evidence.
- `ESS-X02` and `ESS-X03` remain negative controls: unrelated green evidence cannot erase a counterexample, and exact reversion cannot revive authority.

**Intervention**

Replace a timeless architecture tour with a causal reconstruction: failed rule → witness → repair constraint → present mechanism → remaining limit.

**Reader-promise hypothesis**

Kyle can tell which semantics were earned by a witnessed failure, which were merely chosen conservative boundaries, and which remain provisional.

**Form and scale**

Historical architecture RFC; full-RFC scale. The failed moves are the organizing method, not production incident claims.

**Theoretical and evidence gaps**

Applicability (`ESS-T01`), cross-version equivalence (`ESS-T02`), selective expiry and alternate repair (`ESS-T08`), independent range (`ESS-E01`), scale (`ESS-E12`), and release state (`ESS-E13`) remain open.

**Source status**

Mixed historical sequence, provisional design, reproduced local defects, user-authorized repair, implemented prototype behavior, and source-reported tests. No production Endpoints bug or general reliability finding is asserted.

**Injected choices**

- Select three repair episodes as the narrative spine.
- Use chronology as exposition even though the source also supports nonhistorical topologies.
- If Endpoints appears, use one bounded scene after the general repair rule is established; do not make it the origin or validation of the architecture.

**Risks**

Vivid failures may receive excess salience; grouped chronology may blur status changes; current green tests may be mistaken for independent range evidence.

**Relationships**

Shares mechanisms with `ECM-LE-05`; supplies historical context for `ECM-LE-02`; exposes the applicability breakpoint developed in `ECM-LE-03`.

**Reconstruction note**

The claim reconstructs directly from `ESS-H03`, `ESS-S03`, `ESS-S04`, and `ESS-S12`. The interface-level loss of repair evidence comes from `LGI-V2-18`. Gaps and present implementation wording come from `ESS-E01`, `ESS-E13`, and `LGI-V2-19`. No new repair theory is required.

---

## ECM-LE-02 — The base described by its refusals

**Public claim**

The reusable base is partly defined by what it refuses to own: domain truth, consumer permission, universal severity, semantic equivalence, hostile-process security, distributed durability, and production Endpoints policy.

**Upstream inputs and exact support**

- `ESS-S01`: Field Log `3875–3887`; grammar README `9–17, 32–50`; `model.json` `P1/R1/R9`; workflows README `13–28`; prototype README `9–16, 134–154`. Four ownership layers separate base mechanics, claim-package semantics, authoring workflows, and consumer policy.
- `ESS-S05`: Field Log `6169–6225`; grammar README `25–31, 46–50`; Oracle Guide `239–276, 296–310`; `protocol.ts` `1–50`; `kernel.ts` `99–104, 433–465, 570–637`. Missing, unresolved, contradicted, unreachable, and disallowed are distinct.
- `ESS-S11`: `storage.mjs` `6–66`; `kernel.ts` `208–357`; rebuild tests `154–182`; README `96–116`; HANDOFF `42–45, 125–133`. Checksummed local persistence detects corruption but is not hostile-actor protection or independent-writer coordination.
- `ESS-H07`: v2 README `24–27`; v2 boundary conditions; HANDOFF `135–138`; evaluation ledger `67, 73`. Exact identity, global epochs, and same-claim replay are conservative prototype boundaries.
- `ESS-P08`: `storage.mjs` `8–39`; `kernel.ts` `186–189`; README `113–116`; evaluation ledger `77`. The checksum is explicitly not a signature or hostile-process boundary.
- `ESS-T02`, `ESS-T04`, `ESS-T08`, `ESS-T09`: equivalence, policy, richer repair, and consumer fallback remain unresolved or outside the base.
- `ESS-E08`, `ESS-E11`, `ESS-E12`, `ESS-E13`: multi-process coordination, hostile-process controls, scale, and packaged release remain absent or unmeasured.

**Control-ledger status**

- `ESS-H07` remains a supporting shadow with an unresolved-design-space status.
- All `ESS-T*` and `ESS-E*` inputs here constrain the piece; they do not establish a solution.
- `ESS-X07` remains a negative control against inferring a theorem prover, graph database, authentication rewrite, remote authority service, or mandatory independent reviewer.
- `LGI-V2-X01`, `LGI-V2-X03`, and `LGI-V2-X04` remain negative or unresolved: global expiry is conservative rather than necessary, richer matching and durable certificates are unsettled, and graph/security products are not base requirements.

**Intervention**

Use negative space as the explanatory device: ownership boundary, semantic boundary, operational boundary, and evidence boundary.

**Reader-promise hypothesis**

Kyle can distinguish principled separation from unfinished work, and conservative implementation choices from claims that richer designs are impossible.

**Form and scale**

Boundary atlas or compact essay; section scale.

**Theoretical and evidence gaps**

All named `T*` and `E*` items remain return points. The piece supplies no security, equivalence, policy, durability, or scale design.

**Source status**

Supported architecture separation and implemented local mechanisms, surrounded by explicit prototype boundaries and unresolved gaps.

**Injected choices**

- Group boundaries into four editorial families; that grouping is not a source ontology.
- Treat “refusal” as an editorial frame, not a claim that every omission was intentional.
- Use Endpoints only as the worked example of where consumer policy begins.

**Risks**

Negative space may make incomplete implementation look deliberately complete; concise boundary language may erase the difference between rejected scope and unimplemented scope.

**Relationships**

Constrains every other sample; overlaps the worked-example stop conditions in `ECM-LE-06`; frames the open applicability question in `ECM-LE-03`.

**Reconstruction note**

Ownership and state distinctions come from `ESS-S01` and `ESS-S05`; operational limits from `ESS-S11` and `ESS-P08`; conservative semantics from `ESS-H07`; unresolved and unimplemented regions retain their `T*`/`E*` statuses. No absent capability is converted into architectural virtue.

---

## ECM-LE-03 — Who owns applicability, and can it say “unknown”?

**Public question**

Should applicability be base-owned, rule-owned, or layered—and independently, should matching be boolean or explicitly three-valued?

**Upstream inputs and exact support**

- `BR-06`: applicability is a protected unknown across base-owned versus rule-owned matching, boolean versus three-valued results, and a possible layered combination.
- `ESS-T01`: grammar `model.json` `C5/E4/R3`; `kernel.ts` `552–568`; evaluation ledger `67`. The grammar specifies rule-owned captured/requested-context matching with explicit unknown; the kernel implements private base-owned boolean exact matching.
- `ESS-S05`: exact support above. Evidence outcome distinctions are established, while permission and strictness remain separate.
- `ESS-H06`: Field Log `6209–6225`; grammar `source.md` `P16` and rejected recombinations `164–176`; `model.json` `P7/R9/U4`. Missing-evidence strictness was deliberately left open.
- `LGI-V1-05`: root `model.json:C5,E4,R3`; the public contract compresses applicability to fingerprint updates and does not expose condition objects or unknown-match reasons.
- `LGI-V2-07`: v2 `model.json:C5` `109–113`, `E4` `145–148`, `R3` `173–176`; the public operation layer does not expose the rule-owned matcher, side conditions, or explicit per-use unknown.
- `LGI-U01`: opaque service results and prose mean absence from the inspected public contract does not establish runtime absence.
- `ESS-P04`: DESIGN `7–23`; `effect-verdict.mjs` `29–60`; `endpoints-real.mjs` `93–141`; rebuild tests `208–246`. The Endpoints worked case distinguishes pass, fail, and unresolved on supplied summaries.

**Control-ledger status**

- `BR-06` and `ESS-T01` are unresolved decision inputs. They may support the question but no answer.
- `LGI-V1-05` and `LGI-V2-07` are revision-specific supported recoveries, both retaining `compression` or `category mismatch` status.
- `LGI-U01` prevents interface absence from becoming a runtime claim.
- `ESS-P04` is bounded implementation evidence for one worked consumer, not evidence for an applicability architecture.

**Intervention**

Separate two choices often collapsed together—ownership and result cardinality—then show what each existing layer can and cannot currently express without choosing among them.

**Reader-promise hypothesis**

Kyle can evaluate the unresolved decision without having the current kernel implementation silently presented as the answer.

**Form and scale**

Decision-question RFC; full-RFC scale, with an intentionally open conclusion.

**Theoretical and evidence gaps**

The central theory gap is `ESS-T01`. Hidden dependency capture (`ESS-T05`), workflow effectiveness (`ESS-E03`), public interface completion (`ESS-E10`), and independent range (`ESS-E01`) remain open.

**Source status**

Confirmed protected unknown, provisional grammar, smaller implemented kernel rule, revision-specific public-contract loss, and one bounded consumer example.

**Injected choices**

- Present base-owned, rule-owned, and layered arrangements as live families because the frozen brief names all three; do not order them.
- Keep boolean versus three-valued matching as a separate axis.
- Use Endpoints’ unresolved result only to prevent premature conflation of “unknown applicability” with “missing or incomplete evidence.”

**Risks**

Option order may be read as preference; the implemented matcher may acquire default authority; applicability unknown may be conflated with an unresolved evidence verdict.

**Relationships**

Turns one boundary from `ECM-LE-02` into an honest design question; connects to public expressibility in `ECM-LE-04`; can use the bounded scene in `ECM-LE-06`.

**Reconstruction note**

The question and live alternatives are explicitly frozen in `BR-06` and `ESS-T01`. Grammar/interface contrast comes from `LGI-V1-05` and `LGI-V2-07`, constrained by `LGI-U01`. No option is selected or supplemented with new semantics.

---

## ECM-LE-04 — Operational parity, semantic compression

**Public claim**

CLI, LSP, and MCP share one implemented operation path, but transport parity does not mean the public agent contract preserves the grammar’s manipulable objects, authoring operations, explanation status, or audit history.

**Upstream inputs and exact support**

- `ESS-S09`: README `22–95`; `service.mjs` `45–131`; CLI `1–71`; LSP `5–194`; MCP `5–163`; rebuild tests `264–322`; evaluation ledger `57`. Shared-path parity is implemented and locally tested, not independent corroboration.
- `ESS-P06`, `ESS-P07`: current LSP diagnostic mapping and MCP’s six tools are implemented adapter behavior; universal severity and real agent registration are not established.
- `LGI-V1-03`: `model.json:C3,R1`; rules are hard-wired with no public rule schema, inspection, or registration operation.
- `LGI-V1-05`: applicability objects and unknown reasons are compressed.
- `LGI-V1-08`: `model.json:R8,O1`; gap/task projection and its authority limits have no public operation.
- `LGI-V1-29`: grammar `source.md:P31`, `evidence.json:D3`, `source.md:204`; authoring cost, graph-selection bias, and same-analyst correlation vanish from the agent surface.
- `LGI-V2-02`: v2 `model.json:P6/C2`; public history compresses run identity, individual observations, interpretations, and shared run metadata.
- `LGI-V2-09`: v2 README `29–32`, `model.json:R8`; the public contract does not fully declare successful-route versus flat-gap semantics.
- `LGI-V2-11`: investigation work derived from uncovered premises is absent from the operation/type surface.
- `LGI-V2-18`: specific repair controls are compressed to aggregate test/mutation counts.
- `ESS-E03`, `ESS-E10`: authoring workflows and real agent/editor integration remain unmeasured or unfinished.
- `LGI-U01`: opaque results prevent claims of runtime absence.

**Control-ledger status**

- All `LGI-V1-*` and `LGI-V2-*` inputs remain revision-specific supported recoveries with their stated `compression`, `category mismatch`, or `low salience` drop rules.
- They establish public-contract loss only.
- `ESS-E03` and `ESS-E10` are evidence/implementation gaps.
- `ESS-X09` remains a negative control: shared service parity is not independent corroboration.

**Intervention**

Contrast “an agent can invoke an operation” with “an agent can inspect, author, challenge, and preserve the semantics behind it.”

**Reader-promise hypothesis**

Kyle can see the difference between a working adapter layer and an agent-facing design surface without losing credit for the implementation that exists.

**Form and scale**

Annotated public-contract delta or medium essay; section scale.

**Theoretical and evidence gaps**

Applicability (`ESS-T01`), rubric admission (`ESS-T07`), workflow evaluation (`ESS-E03`), failure provenance (`ESS-E04`), and real-client/agent use (`ESS-E10`) stay unresolved.

**Source status**

Implemented local shared service and adapters; revision-specific grammar-to-interface recoveries; public-surface absence only; opaque runtime fields unmeasured.

**Injected choices**

- Organize the comparison by verbs: inspect, author, assess, challenge, repair, and explain.
- Treat interface audit as an editorial form, not a proposal to expose every grammar object.
- Endpoints appears only as the currently fixed worked check behind some operations.

**Risks**

The delta form may imply every loss should be restored; readers may mistake absence from a declared schema for runtime absence; interface detail may crowd out the base’s conceptual purpose.

**Relationships**

Operational counterpart to `ECM-LE-03`; exposes the tooling gap behind `ECM-LE-05`; shares boundary discipline with `ECM-LE-02`.

**Reconstruction note**

Existing parity comes solely from `ESS-S09`, `ESS-P06`, and `ESS-P07`. Semantic compression is reconstructed from the named LGI recoveries while retaining `LGI-U01`. No new public API or restoration recommendation is introduced.

---

## ECM-LE-05 — Evidence as a maintenance practice

**Public claim**

A credible check is not merely a schema or a passing result; it is a maintained chain connecting a declared law and bounded domain to a reference, production path, checkpoint, observations, omissions, reach witness, fault controls, contradiction handling, and replay.

**Upstream inputs and exact support**

- `ESS-S06`: Oracle Guide `17–41, 102–122, 239–276, 312–324`; `protocol.ts` `21–45`; `endpoints-real.mjs` `8–62`; DESIGN `7–68`. The declared check contract shape and one bounded instantiation.
- `ESS-H04`: Oracle Guide `72–108, 124–199, 201–324`; evaluation ledger `54–62`. The Oracle Guide is a portfolio discipline, not merely a `CheckContract` schema.
- `ESS-S08`: Field Log `3881–3887, 4008–4022`; workflows README `1–28`; design-check `1–87`; maintain-evidence `1–33`; repair-rule `1–32`. Three separate authoring and maintenance procedures.
- `ESS-S04`: append-oriented contradiction and replay lifecycle.
- `ESS-H05`: Field Log `4126–4137`; design-check `46–60`; HANDOFF `130–131`. Diagnostic validity and repair-guidance validity are distinct; agent correction evaluation is unmeasured.
- `LFG-H05`: Field Log `2475–2486`; draft result-state and failure-artifact distinctions were lost by `category mismatch`.
- `LFG-H07`: Field Log `2488–2499`; alternate contradiction dispositions were compressed by the prototype’s exact-replay choice.
- `LFG-H14`: Field Log `3219–3238`; reviewed direction for direct durable oracle reports remains unmodeled/open.
- `LFG-H18`: Field Log `71–78, 4130–4137`; donor-study synthesis distinguished finding validity from repair-guidance validity, with no independent evaluation.
- `LFG-H19`: Field Log `5932–5934, 4030–4034, 6321–6329`; checked-in evidence, import verification, durable delivery, and storage were an open implementation boundary.
- `ESS-E03`–`ESS-E07`: agent workflow evaluation, complete failure provenance, machine-readable mutant witnesses, full trace documentation, and fixture-responsibility documentation remain incomplete.

**Control-ledger status**

- `ESS-H04` and `ESS-H05` remain supporting shadows; omission does not award priority.
- Every `LFG-H*` item remains a supported recovered omission with its original claim kind and drop rule. Recovery neither requires restoration nor establishes usefulness.
- `ESS-E03`–`ESS-E07` are gaps, not evidence that the missing controls work.
- `LFG-U01` constrains the whole card: source claims were not independently revalidated, and RFC candidacy remains unmeasured.

**Intervention**

Describe the system through the work required to establish, maintain, challenge, and repair evidence rather than through stored nouns alone.

**Reader-promise hypothesis**

Kyle can evaluate whether the proposed workflows and evidence lifecycle preserve the epistemic controls he cares about, while seeing exactly which workflow claims remain unevaluated.

**Form and scale**

Method RFC or procedural essay; full-RFC scale.

**Theoretical and evidence gaps**

Rule soundness (`ESS-T03`), hidden dependency completeness (`ESS-T05`), alternate repair (`ESS-T08`), workflow effectiveness (`ESS-E03`), provenance completeness (`ESS-E04`), and durable witnesses (`ESS-E05`) remain open.

**Source status**

Normative guide, provisional design, documented workflows, bounded implementation, supported recovered omissions, and explicitly unmeasured agent effectiveness.

**Injected choices**

- Use lifecycle verbs rather than the six grammar primitives as the primary exposition.
- Retain recovered items only where they expose authoring or maintenance cost; do not restore them as required architecture.
- Use the Endpoints check card as one bounded walkthrough of the portfolio, explicitly noting that only a subset is exercised.

**Risks**

Normative guidance may be mistaken for implemented behavior; recovered omissions may be silently promoted; process detail may obscure the smaller logical core.

**Relationships**

Process-oriented complement to the repair history in `ECM-LE-01`; supplies the missing authoring dimension behind `ECM-LE-04`; constrained by `ECM-LE-02`.

**Reconstruction note**

The portfolio claim is directly supported by `ESS-S06` and `ESS-H04`; workflow separation by `ESS-S08`; repair lifecycle by `ESS-S04`; authoring and historical losses by the named `LFG-H*` recoveries. All gaps and recovered statuses stay visible, so no new workflow-efficacy claim enters.

---

## ECM-LE-06 — A worked example that stops where the evidence stops

**Public claim**

A bounded consumer can make the general mechanics concrete without establishing generality, analyzer completeness, production permission, or a universal fallback policy.

**Upstream inputs and exact support**

- `BR-03`: the reusable evidence base is the subject; Endpoints is provenance and a worked consumer.
- `BR-09`: no production Endpoints readiness, broad PostgreSQL support, settled policy, or universal proof claims.
- `ESS-S10`: DESIGN `1–110`; HANDOFF `48–61`; `endpoints-real.mjs` `8–141`; `effect-verdict.mjs` `11–62`; rebuild tests `184–246`; evaluation ledger `56`. One production-shaped worked consumer over pre-analyzed summaries and explicit authority state.
- `ESS-P04`: the same bounded law yields pass, fail, and unresolved outcomes.
- `ESS-E01`: Endpoints helped construct the grammar and cannot validate independent range.
- `ESS-E02`: the small oracle does not establish analyzer completeness, wider PostgreSQL behavior, value correctness, external writes, or refresh policy.
- `ESS-E07`: fixture responsibility is not explicitly documented.
- `ESS-T09`: the consumer fallback contract remains unselected.

**Control-ledger status**

- `ESS-S10` and `ESS-P04` are bounded implementation and test observations.
- `ESS-E01`, `ESS-E02`, and `ESS-E07` are evidence/documentation gaps.
- `ESS-T09` is an unresolved design question.
- `ESS-X06` and `ESS-X08` remain negative controls: full refetch is not universally safe, and Endpoints is not independent evidence of generality.

**Intervention**

Pair every mechanism scene with an explicit stop condition: what the example shows, what the fixture supplies, and what remains outside the claim.

**Reader-promise hypothesis**

Kyle gets a concrete mental model of evidence flow without the demo quietly becoming the architecture or a readiness claim.

**Form and scale**

Worked-example chapter with boundary ledger; section scale.

**Theoretical and evidence gaps**

Independent range, SQL analyzer completeness, fixture responsibility, external writes, authority enforcement, and consumer fallback all remain open.

**Source status**

Bounded current implementation and source-reported tests over supplied summaries; no generality or production-readiness status.

**Injected choices**

- Use the pass/fail/unresolved triad as the only detailed Endpoints scene.
- Put boundary statements adjacent to the scene rather than in a distant limitations section.
- Return immediately to general base mechanics after the example.

**Risks**

The concreteness of Endpoints may still anchor the architecture; “production-shaped” may be misread as production-ready; boundary density may interrupt the narrative.

**Relationships**

Concrete companion to `ECM-LE-02`; supplies a scene for `ECM-LE-03` and `ECM-LE-05`; contrasts with the unsupported generalization rejected as `ECM-LE-R02`.

**Reconstruction note**

The candidate is rebuilt entirely from `BR-03`, `BR-09`, `ESS-S10`, and `ESS-P04`, with `ESS-E01`, `ESS-E02`, `ESS-E07`, `ESS-T09`, `ESS-X06`, and `ESS-X08` enforcing the stopping points. It adds no Endpoints or PostgreSQL claim.

## Rejected generated samples

### ECM-LE-R01 — “A secure proof service for production Endpoints”

**Attempted premise**

Present the evidence base as a hostile-process-secure proof authority ready to govern production Endpoints decisions.

**Rejection reason**

Unsupported and directly out of bounds. `BR-09` forbids hostile-process security, universal proof, and production-readiness claims; `ESS-E11` records absent signatures, authentication, isolation, and controlling-actor provenance; `ESS-X07` and `LGI-V2-X04` keep theorem-prover/authentication-product inferences unavailable as positive support.

### ECM-LE-R02 — “Endpoints proves the architecture generalizes”

**Attempted premise**

Use the real Endpoints package and tests as evidence that the reusable base works across domains.

**Rejection reason**

Directly contraindicated. `BR-03` restricts Endpoints to provenance and worked-consumer status; `ESS-E01` says independent range is untested and Endpoints helped construct the grammar; `ESS-X08` explicitly bars using the vertical as independent generality evidence.

### ECM-LE-R03 — “One confidence score across CLI, LSP, and MCP”

**Attempted premise**

Collapse route support, green runs, adapter agreement, severity, and permission into a single confidence dashboard.

**Rejection reason**

Multiple explicit negative controls apply. `ESS-X01` rejects green counts as universal confidence, `ESS-X04` rejects a generic edge that merges support and permission, and `ESS-X09` rejects adapter parity as independent corroboration. `ESS-T04` leaves severity, permission, confidence aggregation, and priority unresolved.

### ECM-LE-R04 — “How the repairs made evidence reliable”

**Attempted premise**

Retitle the same three repair episodes from `ECM-LE-01` as a reliability story without changing intervention, form, scale, or source basis.

**Rejection reason**

Wording-only duplicate of `ECM-LE-01`; it supplies no distinct editorial coordinate. “Reliable” also exceeds `ESS-E01` and `LGI-V2-19`, which leave independent range and post-repair validation unmeasured, and `ESS-E13`, which records unreleased working-tree state.

## Relationship clusters

- **History and method:** `ECM-LE-01` explains why repair constraints exist; `ECM-LE-05` explains the continuing work those constraints require.
- **Boundary and question:** `ECM-LE-02` inventories protected limits; `ECM-LE-03` opens one central boundary without resolving it.
- **Semantics and operations:** `ECM-LE-03` asks what applicability must mean; `ECM-LE-04` asks what agents can publicly manipulate or inspect.
- **General and concrete:** `ECM-LE-06` can supply a bounded scene for several candidates while remaining subordinate to the general evidence base.
- These are descriptive relationships, not a sequence or ranking.

## Typed handoff coverage

- **Used as positive source support:** `BR-02`, `BR-03`, `BR-04`; `ESS-S01`, `S03`–`S06`, `S08`–`S12`; `ESS-H03`–`H07`; `ESS-P04`, `P06`–`P08`; `LFG-H05`, `H07`, `H14`, `H18`, `H19`; `LGI-V1-03`, `V1-05`, `V1-08`, `V1-29`; `LGI-V2-02`, `V2-07`, `V2-09`, `V2-11`, `V2-18`.
- **Used only as constraints, gaps, protected unknowns, or no-claims:** `BR-06`, `BR-09`; `ESS-T01`–`T05`, `T07`–`T09`; `ESS-E01`–`E13` where named in cards; `LFG-U01`; `LGI-V2-19`; `LGI-U01`.
- **Used only as negative controls:** `ESS-X01`–`X04`, `X06`–`X09`; `LGI-V2-X01`, `V2-X03`, `V2-X04`.
- **Unavailable as positive support throughout:** all `ESS-X01`–`X10`, `LFG-X01`–`X11`, `LGI-V1-X01`–`X14`, `LGI-V2-X01`–`X04`, and unsupported reaches `LGI-V1-U01`–`U02`.
- **Missing input, not null findings:** `ESS-U01`–`U05`; none was used to assert absence.
- **Vote controls retained:** `LFG-C01` and `LGI-C01`; no agreement or disagreement raised confidence or settled conflict.
- **Unused:** every admitted `BR-*`, `ESS-S/H/P/T/E/TOPO-*`, `LFG-H*`, and `LGI-V1/V2-*` item not named above remained unused in this coordinate pass. Unused status carries no merit judgment.
- The source-survey distortion warnings remain active: formal graph relations are easier to see than authoring cost, failures are unusually vivid, Endpoints can anchor generalization, and “shadow” can create an omission bonus.
