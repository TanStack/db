# TanStack Trust RFC — frozen architecture-first evidence v2

**Evidence version:** DG-E2  
**Base evidence:** DG-E1, SHA-256 2c30d28b5d9730198f809ac4015ba8682336895f1c23a628f6b90ca622ba2919  
**Model:** DG-M2  

DG-E2 inherits the complete source, reconstruction, ablation, range, exclusion,
and decomposition-loss evidence in DG-E1. This file records the correction and
the controls affected by it.

## Correction evidence

Kyle's exact correction:

> hmm we already have an endpoints RFC — the idea here is to go deep on the
> architecture of Trust w/ endpoints just illustrating it as needed

This is user-owned target evidence. It changes which system is being modeled
for presentation. It does not change the source RFC's concepts, their statuses,
the implementation, or the Endpoints evidence boundary.

## Affected reconstruction

| Control | DG-M1 result | DG-M2 result |
| --- | --- | --- |
| Product subject | Ambiguously shared between Trust architecture and an expanding Endpoints case | Trust architecture is explicit throughout |
| Endpoints role | Source, product home, bounded evidence, and proposed narrative spine | Product home, bounded evidence, and just-in-time illustration only |
| C11–C16 | Prerequisites for several general authority concepts | Illustration branch dependent on general concepts |
| C17–C65 | Reconstructed partly through safe-skip | Reconstructed directly from the general source sections |
| C66–C72 | Current vertical and limits | Unchanged current vertical and limits |
| Layered explanation | One case expands or a whole-document lifecycle | Every architecture concept receives question, example, rule, and boundary |
| Build graph | Ten units and one target/task conflict | Same units, grouped into four dependency-respecting increments |

All 82 source concepts, 18 invariants, 16 rules, 12 overlaps, seven unresolved
conflicts, ten build units, and twelve preservation properties remain present.

## Affected ablations

- Removing Kyle's correction now breaks the live extraction target by producing
  a second Endpoints-centered document.
- Removing Endpoints entirely still breaks C10–C16 and C66–C72: incubation,
  bounded implementation evidence, the decision oracle, status, and non-range
  classification would disappear.
- Promoting C11–C16 back into the prerequisite path breaks the corrected target
  and O12 by letting constructive evidence organize the architecture.
- Removing just-in-time examples breaks the layered-explanation requirement and
  returns the authority-map comprehension problem.
- Replacing examples with one long Endpoints chapter preserves facts but breaks
  the requested explanatory role.
- Removing the four-depth concept contract makes “layered” a document-level
  slogan rather than a local readability rule.

No unaffected concept, invariant, rule, overlap, or build unit required a new
ablation. DG-PROC1 remains the full item-by-item ledger.

## Range and exclusion

Range remains untested. Endpoints cannot be promoted from constructive example
to independent range evidence by using it more frequently in the RFC.

The DG-E1 latest-status-dashboard negative remains excluded. DG-M2 adds one
presentation exclusion: a document whose main plot is the safe-skip feature and
whose conclusion generalizes to Trust is outside the corrected target, even if
its individual claims are accurate.

## Adjacent-form control

The three DG-M2 forms differ structurally:

- AF01 uses architectural modules as the document spine.
- AF02 uses reader questions as the document spine.
- AF03 repeats the whole architecture at increasing precision.

The prior F01 expanding Endpoints case is rejected under the correction. The
prior F02 lifecycle survives only if the lifecycle is generalized and
subordinated to architecture; that result is covered by AF02. The prior F03
becomes AF03 after removing Endpoints as its first-layer subject.

## Decomposition-loss retest

DG-E1 identified the felt human burden, coupled one-product shape, temporal
story, and status rhythm as whole-text properties. All survive:

- the human burden opens Layer 1;
- one Endpoints product still contains separate authorities;
- the temporal architecture receives its own deep layer; and
- every concept's boundary depth includes status and limits.

The correction adds a fifth whole-text requirement: **architectural
centrality**. Repeated examples must clarify the system without becoming the
system's implied definition.

## Limits

- The architecture-first teaching order is still an analyst-generated model,
  not human-reader evidence.
- Other TanStack examples remain unchecked illustrations.
- The new form does not decide applicability, operational-failure storage,
  trust-root governance, or any other unresolved design boundary.
- The correction does not establish that a Trust-focused RFC should be longer,
  more abstract, or separately packaged.
